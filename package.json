{
  "name": "docap-monorepo",
  "private": true,
  "workspaces": [
    "doctor_Apointment",
    "doctor_Apointment/client",
    "admin-panel"
  ],
  "scripts": {
    "postinstall": "npm install --prefix doctor_Apointment/client && npm install --prefix admin-panel",
    "build": "npm --prefix doctor_Apointment/client run build && npm --prefix admin-panel run build",
    "start": "node doctor_Apointment/server.js"
  }
}
