{
  "name": "docap-monorepo",
  "private": true,
  "workspaces": [
    "doctor_Apointment",
    "doctor_Apointment/client",
    "admin-panel"
  ],
  "scripts": {
    "build": "npm install --prefix doctor_Apointment/client --include=dev && npm install --prefix admin-panel --include=dev && npm --prefix doctor_Apointment/client run build && npm --prefix admin-panel run build",
    "start": "node doctor_Apointment/server.js"
  }
}
